frase = "Subi no pé de lima para ver o meu amor passar..."
lista_corte = frase.split()
frase_corte = []

for i, frase in enumerate(lista_corte):
    frase_corte.append(lista_corte)
    print(lista_corte[i])